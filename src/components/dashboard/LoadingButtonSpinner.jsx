"use client";

import React from "react";
import { ClipLoader } from "react-spinners";

export default function LoadingButtonSpinner() {
  return <ClipLoader color="#000" size={12} speedMultiplier={1} />;
}
